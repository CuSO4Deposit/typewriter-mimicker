[project]
name = "renderer"
version = "0.1.0"
description = "Add your description here"
readme = "README.txt"
requires-python = ">=3.13"
license = "Apache-2.0"
dependencies = [
    "pillow>=12.0.0",
]

[dependency-groups]
dev = [
    "pytest>=8.4.0",
    "ruff>=0.14.0",
]
